# front/ — placeholder

The local web UI (Clay-like table view) is not part of v1. See ROADMAP.md.
When it lands, the server must import `tools/core/db.py` and
`tools/core/workspace.py` directly so the UI never drifts from what the
skills write.
