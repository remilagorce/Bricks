# Bricks front

The local web UI — a Clay-like table in your browser.

Planned stack: local server + React + a data-grid component, reading from the local Bricks database. Launched with `bricks serve`.

> Status: design phase. Nothing to run here yet.